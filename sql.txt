CREATE TABLE `ikwell`.`maps` (
  `id` INTEGER UNSIGNED NOT NULL DEFAULT 0,
  `Lat` REAL NOT NULL DEFAULT 0,
  `Lng` REAL NOT NULL DEFAULT 0,
  `Title` VARCHAR(50) NOT NULL DEFAULT 'Обьект',
  `zoom` INTEGER UNSIGNED NOT NULL DEFAULT 0,
  PRIMARY KEY (`id`)
)
ENGINE = MyISAM;